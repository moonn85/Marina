# Marina Backend

NestJS API for the Marina website.

## Scope

- No database is required.
- Only standalone API modules are included.
- Active modules:
  - `custom-itinerary`
  - `checkin-document`
  - `competitor-prices`
  - root health endpoint

## Commands

```bash
npm install
npm run start:dev
npm run build
```
